version: '3.8'

services:
  agent-office:
    build: .
    ports:
      - "8080:8080"
    environment:
      - STAR_BACKEND_PORT=19001
      - PORT=8080
      - STAR_DATA_DIR=/app/data
    volumes:
      - office-data:/app/data
    restart: unless-stopped

volumes:
  office-data:
